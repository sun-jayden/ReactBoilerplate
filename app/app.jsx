var React = require('react');
var ReactDOM = require('react-dom');

// kick off all of our application
ReactDOM.render (
  //passing in prop into a component
  <h1>Boilerplate app!</h1>,
  document.getElementById('app')
);
